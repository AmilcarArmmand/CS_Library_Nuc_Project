import { users } from './user.js';

// Drizzle needs an object to map table names for the query API
export const schema = {
  users,
  // books,
  // authors,
  // categories,
  // loans,
  // sessions,
};
